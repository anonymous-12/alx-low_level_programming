Malloc and Free are both functions for memorry allocation and deallocation respectively. 
malloc returns a void pointer that gives the address of the first byte in ablock 
free is used to return the allocated memory back to the OS when it is no longer needed
Valgrind 
