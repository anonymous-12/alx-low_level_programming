#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: a string to be compared
 * @s2: same as above
 * Return: return -1
 */
int _strcmp(char *s1, char *s2)
{
	int i = 0;

	while (*a != '\0' && *b != '\0')
	{
		if (*a != *b)
		{
			i = 1;
		}
		a++;
		b++;
	}
}
